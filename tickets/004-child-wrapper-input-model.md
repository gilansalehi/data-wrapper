# Ticket 004: Child Wrapper Input Model

**COMPLETED.**

Shipped: child-wrapper inputs delivered to the factory via `context.props`, the
local-first scope-climb resolver, the `/key` root-scope escape, and the
resolution miss policy. The design lives in
`views/docs/{tokens,contexts,factory,limitations}.html`, `README.md`, and
`testing.md`; contract tests are in `tests/`.
